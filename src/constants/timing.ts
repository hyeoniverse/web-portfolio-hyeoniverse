/** 상태 메시지 자동 사라짐 (ms) — 저장 완료/오류 메시지 dismiss */
export const STATUS_MESSAGE_DISMISS_MS = 3000;

/** 검색 입력 디바운스 (ms) */
export const SEARCH_DEBOUNCE_MS = 300;

/** "복사됨" 피드백 표시 시간 (ms) */
export const COPY_FEEDBACK_MS = 2000;

/** 사이트가 날짜를 그리는 시간대. 서버(UTC)와 브라우저가 같은 날짜를 그려야 미리 그린 HTML 과 하이드레이션이 어긋나지 않는다 */
export const SITE_TIME_ZONE = "Asia/Seoul";
