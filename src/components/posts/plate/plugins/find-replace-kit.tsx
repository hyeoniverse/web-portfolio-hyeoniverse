"use client";

// ── Find & Replace Kit ──
// FindReplacePlugin: search 옵션과 일치하는 텍스트에 search_highlight 데코레이션(하이라이트).
// 검색/이동/치환 UI 는 FindReplaceBar 가 처리.

import { FindReplacePlugin } from "@platejs/find-replace";

export const FindReplaceKit = [FindReplacePlugin];
